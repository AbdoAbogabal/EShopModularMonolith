version: '3.4'

services:
 eshopdb:
  image: postgres
 
 distriputedcache:
  image: redis

 seq:
    image: datalust/seq:latest

 messagebroker:
  image: rabbitmq:management
 
 identity:
   image: quay.io/keycloak/keycloak:24.0.3

volumes:
  postgres_eshopdb:
